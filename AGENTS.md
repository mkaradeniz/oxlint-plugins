# Repository Instructions

- Use Conventional Commits with scopes, such as `whitespace`, `react-structure`, `config`, `ci`, `release`, or `docs`.
- For publishing, release, versioning, npm provenance, or package tag work, use the repo `oxlint-plugins` skill first.
